language: node_js
node_js:
  - "12"

# https://docs.travis-ci.com/user/multi-os/
os:
  - linux
  - osx
dist: xenial

# install:
#     - npm install -g @quasar/cli

# before_deploy:
#     # we need to create a zip first..
# deploy:
#   provider: releases
#   api_key: GH_TOKEN
#   # file: "FILE TO UPLOAD"
#   skip_cleanup: true
#   draft: true

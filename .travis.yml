language: node_js
node_js:
    - "12"
cache: yarn

# https://docs.travis-ci.com/user/multi-os/
os:
    - linux
    - osx
    - windows
    # be carefull - secrets are not protected..!!!
    # https://travis-ci.community/t/current-known-issues-please-read-this-before-posting-a-new-topic/264/22

dist: xenial

jobs:
    allow_failures:
        - os: windows

addons:
    apt:
        packages:
            # needed for node-hid
            # https://www.npmjs.com/package/node-hid#compiling-from-source
            # https://github.com/node-hid/node-hid/pull/253
            - libusb-1.0-0
            - libusb-1.0-0-dev
            - libudev-dev
    # homebrew:
    #     packages: packagename
    # choco:
    #     packages: python2

before_script:
    - if [ $TRAVIS_OS_NAME = windows ]; then epxort YARN_GPG=no; fi
before_install:
    - if [ $TRAVIS_OS_NAME = windows ]; then python2 --version; fi
    # - if [ $TRAVIS_OS_NAME = windows ]; then npm install --global windows-build-tools; fi
    # ^ this does not work.. endless loop...

# build without upload
# after_success:
    # - yarn build

# build and upload
# this is all we need to trigger the upload...
# $GH_TOKEN has to be available...
after_success:
    - yarn release

# deploy:
#     skip_cleanup: true
#     provider: releases
#     api_key: $GH_TOKEN
#     # file: "FILE TO UPLOAD"
#     draft: true
#     on:
#         tags: true
# deploy:
#     skip_cleanup: true
#     provider: script
#     script: yarn release

after_deploy:
    - ps -ef

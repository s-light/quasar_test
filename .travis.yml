language: node_js
node_js:
    - "12"

# https://docs.travis-ci.com/user/multi-os/
os:
    - linux
    - osx
dist: xenial

before_deploy:
    # we need to create a zip first..
    # - yarn create_zips
deploy:
    provider: releases
    api_key: GH_TOKEN
    # file: "FILE TO sUPLOAD"
    skip_cleanup: true
    draft: true
    on:
        tags: true
